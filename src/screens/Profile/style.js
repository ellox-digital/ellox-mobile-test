import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        width: '100%', 
    },
    infosContainer: {
        flex: 1,
        width: '100%',
        borderWidth: 1,
    },
    addressContainer: {
        paddingLeft: 0,
    }
})

export default styles